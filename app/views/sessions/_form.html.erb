<%= form_with scope: :session, url: login_path, local: true do |f| %>
    <p>
        <%= f.label :username %><br>
        <%= f.text_field :username %>
    </p>
    <p>
        <%= f.submit 'Log In' %>
    </p>
<% end %>

<%= link_to 'Cancel and return to event listing', events_path %>